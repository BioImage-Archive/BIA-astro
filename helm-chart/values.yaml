replicaCount: 1

image:
  tag: "0.1.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  limits:
    memory: "256M"
    cpu: ".25"

env:
  publicSearchApi: "http://localhost:8081/search"
  publicMongoApi: "http://localhost:8081/api"
  