---
import BaseLayout from "../../../../layouts/BaseLayout.astro";

import BenchmarkedDatasetPage from "../../../../components/ai-benchmarking/BenchmarkedDatasetPage.astro"

import collection_list from "../../../../data/collections/ai-benchmark-datasets.json"
import study_metadata from "../../../../data/bia-study-metadata.json"
import additional_study_metadata from "../../../../data/ai-dataset-benchmarking/additional-study-metadata.json"
import table_info from "../../../../data/ai-dataset-benchmarking/model-dataset-table.json"


export function getStaticPaths() {
  return Object.values(collection_list["accession_ids"]).map((id) => {
    return {
      params: { accession_id: id },
    };
  });
}

const { accession_id } = Astro.params;

---

<BaseLayout pageTitle={accession_id}>
  <BenchmarkedDatasetPage accession_id={accession_id} study_metadata={study_metadata} additional_study_metadata={additional_study_metadata} table_info={table_info}/>
</BaseLayout>


