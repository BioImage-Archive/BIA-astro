---
import BaseLayout from "../layouts/BaseLayout.astro";
import GalleryCard from "../components/cards/GalleryRoundImageCard.astro";
import galleries from "../data/ai-playground-galleries.json";
---

<BaseLayout pageTitle="AI Playground">
    <body>
        <section
            class="vf-news-container vf-news-container--featured | vf-stack"
        >
            <div class="vf-section-header">
                <h2 class="vf-section-header__heading">
                    <br />
                    {galleries.title}
                </h2>
            </div>
            <div class="vf-news-container__content | vf-grid vf-grid__col-4">
                {
                    Object.values(galleries.collections).map((collection) => (
                        <GalleryCard collection={collection} link_path="ai-playground/"/>
                    ))
                }
            </div>
        </section>
    </body>
</BaseLayout>